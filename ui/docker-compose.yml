version: "2.4"
services:
  mockServer:
    image: mockserver/mockserver:6.1.0
    ports:
      - 1080:1080
    environment:
      MOCKSERVER_INITIALIZATION_JSON_PATH: /config/mocks.json
    volumes:
      - type: bind
        source: ./mock
        target: /config

